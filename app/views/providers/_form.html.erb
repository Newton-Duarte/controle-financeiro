<%= form_for(@provider) do |f| %>
  <% if @provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@provider.errors.count, "error") %> prohibited this provider from being saved:</h2>

      <ul>
      <% @provider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <%= f.label :nome_razao_social %><br>
        <%= f.text_field :nome_razao_social, class: "form-control", placeholder: "Nome ou Razão Social" %>
      </div>
    </div>
  </div>

  <div class="form-group">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <%= f.label :apelido_fantasia %><br>
        <%= f.text_field :apelido_fantasia, class: "form-control", placeholder: "Apelido ou Fantasia" %>
      </div>
    </div>
  </div>

  <div class="form-group">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <%= f.label :cpf_cnpj %><br>
        <%= f.text_field :cpf_cnpj, class: "form-control", placeholder: "CPF ou CNPJ" %>
      </div>
    </div>
  </div>

  <div class="form-group">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <%= f.label :provider_type %>
        <%= f.select :provider_type, options_for_person_types(@provider.provider_type), {}, class: "form-control" %>
      </div>
    </div>
  </div>


  <div class="text-center">
    <%= link_to "Voltar", providers_path, class: "btn btn-danger" %>
    <%= f.submit "Confirmar", class: "btn btn-success" %>
  </div>
<% end %>
